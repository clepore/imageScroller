This is a simple horizontal scroller. It can show 1 or more items in a list at a time, and show multiple scrolling lists on a page without interfence.

Recently I encorporated hash tag navigation for html5 enabled browsers with version 1.0.